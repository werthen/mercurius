<h1><%= @lecturer.name %></h1>
<%= t(:page) %>: <%= link_to @lecturer.url, @lecturer.url %>
<ul>
    <% @lecturer.courses.each do |course| %>
        <li><%= link_to course.name, course %></li>
    <% end %>
</ul>
